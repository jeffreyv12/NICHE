{
  "name": "@nichefinder/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "echo 'no tests yet' && exit 0",
    "db:migrate": "echo 'db:migrate not yet wired — Phase 1.2' && exit 0"
  }
}
