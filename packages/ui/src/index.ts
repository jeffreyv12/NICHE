// @nichefinder/ui — shadcn-based shared components (public + admin + disclosures).
// Phase 1.1 placeholder. Real components land in Phase 1.4+ alongside apps/web.
// Depends on @nichefinder/shared per FOLDER_STRUCTURE.md dependency rules.

export {};
