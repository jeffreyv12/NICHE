// @nichefinder/agent-sdk — Anthropic SDK wrapper, runAgent(), budget guards,
// tier-routing, MCP glue, batch API helpers.
// Phase 1.1 placeholder. Real surface lands in Phase 1.5 (runAgent + guards),
// Phase 2.2 (discovery agent), Phase 2.3 (scoring), etc.
// Depends on @nichefinder/shared + @nichefinder/db per FOLDER_STRUCTURE.md.

export {};
