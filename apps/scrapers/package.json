{
  "name": "@nichefinder/scrapers",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'dev not yet wired — Phase 2.1' && exit 0",
    "build": "tsc -p tsconfig.json --emitDeclarationOnly false || true",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run"
  },
  "dependencies": {
    "@nichefinder/agent-sdk": "workspace:*",
    "@nichefinder/db": "workspace:*",
    "@nichefinder/shared": "workspace:*",
    "drizzle-orm": "0.36.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "@vitest/coverage-v8": "^2.1.9",
    "tsx": "4.19.2",
    "vitest": "^2.1.9"
  }
}
